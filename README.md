# Check Champ 2025 🚀

Этот скрипт предназначен для проверки выполнения заданий в рамках чемпионата по профессиональному мастерству "Профессионалы" 2025 года.

---

## 📥 Установка

1. **Клонируйте репозиторий** (опционально):
   ```bash
   git clone https://github.com/rijkzy/check_champ2025.git
   cd check_champ2025
🚀 Использование
Запуск скрипта одной командой
Вы можете запустить скрипт прямо из терминала, выполнив следующую команду:

bash
Copy
(b=main opts=(check_champ2025.sh) ; echo -e "\e[1;34mСкачивание скрипта...\e[0m" && curl -sfOL "https://raw.githubusercontent.com/rijkzy/check_champ2025/$b/${opts[0]}" && { echo -e "\e[1;32mСкрипт успешно скачан!\e[0m"; chmod +x ${opts[0]}; echo -e "\e[1;34mЗапуск скрипта...\e[0m"; ./"${opts[@]}"; rm -f ${opts[0]}; echo -e "\e[1;32mСкрипт выполнен!\e[0m"; :; } || echo -e "\e[1;31m\nОшибка скачивания: проверьте подключение к Интернету, настройки DNS, прокси и URL адрес\ncurl exit code: $?\n\e[m" >&2)
Скопируйте команду с помощью кнопки
Нажмите кнопку ниже, чтобы скопировать команду в буфер обмена:

<button onclick="copyCommand()">📋 Скопировать команду</button>

<script> function copyCommand() { const command = `(b=main opts=(check_champ2025.sh) ; echo -e "\\e[1;34mСкачивание скрипта...\\e[0m" && curl -sfOL "https://raw.githubusercontent.com/rijkzy/check_champ2025/$b/${opts[0]}" && { echo -e "\\e[1;32mСкрипт успешно скачан!\\e[0m"; chmod +x ${opts[0]}; echo -e "\\e[1;34mЗапуск скрипта...\\e[0m"; ./"${opts[@]}"; rm -f ${opts[0]}; echo -e "\\e[1;32mСкрипт выполнен!\\e0m"; :; } || echo -e "\\e[1;31m\\nОшибка скачивания: проверьте подключение к Интернету, настройки DNS, прокси и URL адрес\\ncurl exit code: $?\\n\\e[m" >&2)`; navigator.clipboard.writeText(command).then(() => { alert("Команда скопирована в буфер обмена! Вставьте её в терминал."); }); } </script>
📝 Описание скрипта
Скрипт выполняет следующие задачи:

Проверяет настройку сетевых устройств.

Проверяет корректность конфигурации VLAN, NAT, DHCP, OSPF и других параметров.

Выводит результаты проверки в удобном формате.

🛠️ Технические детали
Язык: Bash

Зависимости: curl, git, chmod

Поддержка: ALT PVE, Linux
